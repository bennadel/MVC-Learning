
component
	output="false"
	hint="I provide task functionality."
	{


	// I return an initialized component.
	function init( Any util ){

		// Return this object reference.
		return( this );

	}


	// Some top-level functions that we may need:
	//
	// - getTasks( userID )
	// - getTask( userID )
	// - createTask( ... )
	// - updateTask( ... )
	// - deleteTask( ... )
	// - getNotes( taskID )
	// - 


}